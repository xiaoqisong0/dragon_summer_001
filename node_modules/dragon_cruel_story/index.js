// 引入jQuery  const $ = require('jquery');    // 问题function printProblemMessage() {    var q = '别人说我太温柔了，所以我把裤子剪坏' 
  console.log(q)}  

// 答案
function printAnswerMessage() {  
  // 例如，在DOM加载完成后打印欢迎消息  
  var a = '从此做一个残裤的人。' 
  console.log(a)
}   // 暴露函数作为模块的公开API  module.exports = {
	printProblemMessage,
	printAnswerMessage
}; 