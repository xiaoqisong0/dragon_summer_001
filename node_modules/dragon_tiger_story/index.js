// 引入jQuery  const $ = require('jquery');    // 问题function printProblemMessage() {    var q = '动物园里老虎把狮子绿了，为什么呢？' 
  console.log(q)}  

// 答案
function printAnswerMessage() {  
  // 例如，在DOM加载完成后打印欢迎消息  
  var a = '因为老虎有绿狮资格证。' 
  console.log(a)
}   // 暴露函数作为模块的公开API  module.exports = {
	printProblemMessage,
	printAnswerMessage
}; 