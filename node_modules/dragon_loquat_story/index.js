// 引入jQuery  const $ = require('jquery');    // 问题function printProblemMessage() {    var q = '请问枇杷的做法有那些？' 
  console.log(q)}  

// 答案
function printAnswerMessage() {  
  // 例如，在DOM加载完成后打印欢迎消息  
  var a = '有两种。油爆和拌面，因为犹抱琵琶半遮面。' 
  console.log(a)
} 。  // 暴露函数作为模块的公开API  module.exports = {
	printProblemMessage,
	printAnswerMessage
}; 