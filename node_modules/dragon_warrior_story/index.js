// 引入jQuery  const $ = require('jquery');    // 问题function printProblemMessage() {  	var q = '一位勇士躺在床上，后背很痒，但是他不能翻身' 
  console.log(q)}  

// 答案
function printAnswerMessage() {  
  // 例如，在DOM加载完成后打印欢迎消息  
	var a = '因为永世不得翻身。' 
  console.log(a)
}   // 暴露函数作为模块的公开API  module.exports = {
	printProblemMessage,
	printAnswerMessage
}; 